apiVersion: v1
kind: Service
metadata:
  name: production-app-svc
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: production-app
  ports:
  - name: http
    port: 80
    targetPort: 8080
